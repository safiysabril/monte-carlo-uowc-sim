"""uowc - research-grade Monte Carlo UOWC simulation framework.

Ports-and-adapters layout: `core` defines domain types + Protocols (ports);
layer packages are adapters implementing those ports; `experiments` is the
composition root. No concrete class inherits from another concrete class."""
