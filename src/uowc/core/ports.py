"""All Protocol interfaces (ports), per the approved architecture. Contracts only
- no implementations:
  OpticalPropertyModel, PhaseFunction, ScalarField, ChlorophyllProfile,
  HomogenizationRule, EnvironmentalEffect, Boundary, Medium, Rng, Transport,
  Estimator, Tally, Metric, ComparativeMetric, ResultStore."""
